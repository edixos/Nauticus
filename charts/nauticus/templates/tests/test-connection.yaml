apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nauticus.fullname" . }}-test-connection"
  labels:
    {{- include "nauticus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nauticus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
